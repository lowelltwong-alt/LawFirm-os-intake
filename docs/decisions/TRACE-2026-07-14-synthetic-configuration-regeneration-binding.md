# TRACE: Synthetic Configuration Regeneration Binding

Bind a review-ready synthetic configuration change package to fresh baseline and
candidate guideline-projection rebuilds. The audit compares the projection source
manifests with the candidate source hashes and blocks changed sources that the
projection does not consume. It is provenance proof only: it does not authorize
budget calculation, import, submission, or external writes.
