# Digital Asset Directory Handoff

Central hub: `${DAD_HUB}`

Enable the versioned hook for this clone:

```powershell
git config core.hooksPath .githooks
```

Use the preflight/postflight commands in `AGENTS.md`.
